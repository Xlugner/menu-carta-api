---
export interface Props {
  price: number;
}

const { price } = Astro.props;


---

<div class="flex items-center justify-between mt-3">
  <span class="text-2xl font-bold text-ec-pink">${price.toFixed(2)}</span>
</div>
