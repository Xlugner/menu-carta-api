---
export interface Props {
  openingHours?: {
    days: string;
    hours: string;
  };
}

const { 
  openingHours = {
    days: "Lunes a Viernes",
    hours: "9:00 - 18:00"
  }
} = Astro.props;
---

<div class="text-center md:text-left">
  <h3 class="text-sm font-semibold text-white mb-2">Horario de Atención</h3>
  <div class="text-gray-300 space-y-1">
    <p>{openingHours.days}</p>
    <p>{openingHours.hours}</p>
  </div>
</div>